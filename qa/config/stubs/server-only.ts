/**
 * Test stub for the `server-only` package.
 *
 * The real module throws when imported outside a React Server Component build,
 * which would break unit/integration tests that import server modules (the
 * Strapi client, mappers, and feature `api` layers). Aliased in
 * `qa/config/vitest.config.ts`. Intentionally empty.
 */
export {}
